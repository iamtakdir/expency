// stream.js polyfill
import { Readable } from 'readable-stream';

// Export everything from readable-stream
export * from 'readable-stream';
// Export Readable as default for direct imports
export default Readable;
